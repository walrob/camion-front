shamefully-hoist=true
strict-peer-dependencies=false
# El lock se generó con legacy-peer-deps. Sin esto, `npm ci` en CI resuelve las
# peer deps y falla porque el lock no las tiene ("Missing X from lock file").
legacy-peer-deps=true
